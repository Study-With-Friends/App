import styled from 'styled-components';

const ContentWide = styled.div`
    display: flex;
    flex-flow: column;
    margin: 0 auto;
    max-width: 600px;
    width: 100%;
    height: 100%;
`;

export default ContentWide;
